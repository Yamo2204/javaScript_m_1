'use strict';
const number : any[] = [];
for (let i : number = 0; i < 5; i++) {
    numbers.push(+prompt(message: 'Enter a number'));
}

for (let i:number = 4; i >= 0; i--) {
    console.log(numbers[i]);
}